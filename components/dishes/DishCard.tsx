import type { DishProps } from "@/types"

type Props = {
    data : DishProps[]
}

export function DishCard({data}:Props){
    return(
        <div>
            <h1>dishcard here</h1>
        </div>
    )
}